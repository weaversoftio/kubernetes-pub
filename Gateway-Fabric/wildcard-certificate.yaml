apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: dcs-wildcard-cert
  namespace: nginx-gateway
spec:
  secretName: dcs-tls-certificate
  commonName: "*.dcs.local"
  dnsNames:
    - "*.dcs.local"
  issuerRef:
    name: cluster-ca
    kind: ClusterIssuer


